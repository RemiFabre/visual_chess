# Visual Chess — Prototype Status

This file is the single point of entry. Anything you'd want to look at is linked from here. It gets updated as work progresses.

## The idea
Chess is a visual game, but standard boards expose almost none of the visual information beginners need to internalize: who controls what square, where attack lines run, which pieces are pinned, who is overloaded defending what. This repo prototypes four visualizations layered onto a standard board, with the goal of dropping them in front of r/chessbeginners and asking "does seeing the board this way help?"

## How to view
Open `index.html` directly in a browser (no build step). The page has a tab for each visualization, a position picker (curated FENs), and a free FEN input.

- **Live demo (GitHub Pages):** [pending — will enable after first viz lands]
- **Local:** `open index.html` from this directory

## Visualizations

| # | Name | Status | Notes |
|---|------|--------|-------|
| 1 | Controlled space (heatmap) | planned | Square color = controller; split-square when contested; intensity = attacker count |
| 2 | Attack paths | planned | Piece-specific lines, dotted past first collision for sliders |
| 3 | Pins & protection | planned | Ice overlay for pinned pieces, solid/ghostly outlines for protected/exposed |
| 4 | Thought bubbles | planned | Comic bubble per piece showing what it defends; queens visibly overloaded |

## Sprites
Generated by `scripts/generate_sprites.py` using OpenAI `gpt-image-1` (image gen v2). Results land in `sprites/`. The script is idempotent — re-running won't overwrite existing PNGs unless `--force` is passed.

## Curated demo positions
See `js/positions.js`. The intent is one or two positions per visualization where the viz reveals something a beginner would otherwise miss.

## Open questions for the user
*(none yet — will populate here as design decisions come up)*

## Reddit post draft
*(to write after the prototypes look good)*

## Commit log
Run `git log --oneline` for the full picture. Each viz lands in its own commit (or a few).
